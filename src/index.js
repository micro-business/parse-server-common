import {
  BaseObject,
} from './schema';
import {
  ParseWrapperService,
} from './parse-wrapper-service';

export {
  BaseObject,
  ParseWrapperService,
};

export default {
  BaseObject,
  ParseWrapperService,
};
