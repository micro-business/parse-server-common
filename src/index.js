import {
  BaseObject,
} from './schema/base-object';
import {
  ParseWrapperService,
} from './parse-wrapper-service';

export {
  BaseObject,
  ParseWrapperService,
};

export default {
  BaseObject,
  ParseWrapperService,
};
